/**
 * Static tenant used by the dev auth bypass.
 * The ID must stay stable across sessions so React Query keys stay consistent.
 */
export const mockTenantId = '00000000-0000-0000-0000-000000000001';
export const mockTenantName = 'Demo Farm (Mock)';
