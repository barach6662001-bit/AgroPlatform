diff --git a/frontend/src/components/EyebrowChip.module.css b/frontend/src/components/EyebrowChip.module.css
new file mode 100644
index 0000000..61517e2
--- /dev/null
+++ b/frontend/src/components/EyebrowChip.module.css
@@ -0,0 +1,41 @@
+.chip {
+  display: inline-flex;
+  align-items: center;
+  gap: 8px;
+  padding: 4px 11px;
+  background: rgba(255, 255, 255, 0.025);
+  border: 1px solid rgba(255, 255, 255, 0.08);
+  border-radius: 999px;
+  width: fit-content;
+}
+
+.dot {
+  width: 6px;
+  height: 6px;
+  border-radius: 50%;
+  background: var(--acc);
+  box-shadow: 0 0 12px rgba(var(--accRgb), 0.85);
+  flex-shrink: 0;
+}
+
+.pulse {
+  animation: chipPulse 2s ease-in-out infinite;
+}
+
+.label {
+  font-family: var(--font-mono, ui-monospace, monospace);
+  font-size: 11px;
+  font-weight: 500;
+  letter-spacing: 0.08em;
+  text-transform: uppercase;
+  color: var(--text-secondary);
+}
+
+@keyframes chipPulse {
+  0%, 100% { opacity: 1; transform: scale(1); }
+  50%      { opacity: 0.55; transform: scale(0.85); }
+}
+
+@media (prefers-reduced-motion: reduce) {
+  .pulse { animation: none; }
+}
diff --git a/frontend/src/components/EyebrowChip.tsx b/frontend/src/components/EyebrowChip.tsx
new file mode 100644
index 0000000..04fe6c4
--- /dev/null
+++ b/frontend/src/components/EyebrowChip.tsx
@@ -0,0 +1,15 @@
+import s from './EyebrowChip.module.css';
+
+interface Props {
+  label: string;
+  pulse?: boolean;
+}
+
+export default function EyebrowChip({ label, pulse = true }: Props) {
+  return (
+    <span className={s.chip}>
+      <span className={`${s.dot} ${pulse ? s.pulse : ''}`} />
+      <span className={s.label}>{label}</span>
+    </span>
+  );
+}
diff --git a/frontend/src/components/Layout/AppLayout.module.css b/frontend/src/components/Layout/AppLayout.module.css
index a177964..bfbf307 100644
--- a/frontend/src/components/Layout/AppLayout.module.css
+++ b/frontend/src/components/Layout/AppLayout.module.css
@@ -22,10 +22,10 @@
 
 /* ── Sidebar shell ── */
 .sidebar {
-  width: 216px;
+  width: 232px;
   flex-shrink: 0;
-  background: var(--bg-page);
-  border-right: 1px solid var(--border);
+  background: rgba(255, 255, 255, 0.02);
+  border-right: 1px solid rgba(255, 255, 255, 0.06);
   display: flex;
   flex-direction: column;
   overflow: hidden;
@@ -42,14 +42,13 @@
   flex: 1;
   overflow-y: auto;
   overflow-x: hidden;
-  padding: 4px 8px;
+  padding: 8px 8px 8px;
 }
 
 /* ── Logo area ── */
 .logoWrap {
-  padding: 14px 16px 12px;
-  border-bottom: 1px solid var(--border);
-  margin-bottom: 4px;
+  height: 56px;
+  padding: 0 16px;
   display: flex;
   align-items: center;
   flex-shrink: 0;
@@ -61,22 +60,21 @@
   content: '';
   position: absolute;
   bottom: 0;
-  left: 16px;
-  right: 16px;
+  left: 12px;
+  right: 12px;
   height: 1px;
-  background: linear-gradient(90deg, var(--brand) 0%, transparent 80%);
-  opacity: 0.2;
+  background-image: linear-gradient(90deg, rgba(var(--accRgb), 0.4), transparent 78%);
 }
 
 .logoWrapCollapsed {
-  padding: 16px 0;
+  padding: 0;
   justify-content: center;
 }
 
 /* ── User section at bottom ── */
 .userSection {
-  padding: 10px 12px;
-  border-top: 1px solid var(--border);
+  padding: 12px 12px;
+  border-top: 1px solid rgba(255, 255, 255, 0.06);
   display: flex;
   align-items: center;
   gap: 10px;
@@ -86,18 +84,18 @@
 }
 
 .userSection:hover {
-  background: var(--bg-hover);
+  background: rgba(255, 255, 255, 0.03);
 }
 
 .userSectionCollapsed {
-  padding: 10px 0;
+  padding: 12px 0;
   justify-content: center;
 }
 
 .userAvatar {
   width: 30px;
   height: 30px;
-  background: linear-gradient(135deg, var(--brand) 0%, var(--brand-hover) 100%);
+  background: linear-gradient(135deg, var(--acc) 0%, var(--acc2) 100%);
   border: none;
   border-radius: 50%;
   display: flex;
@@ -107,7 +105,7 @@
   color: #000;
   font-weight: 700;
   flex-shrink: 0;
-  box-shadow: 0 0 0 2px rgba(34,197,94,0.15);
+  box-shadow: 0 0 0 2px rgba(var(--accRgb), 0.15);
 }
 
 .userMeta {
@@ -125,11 +123,12 @@
 }
 
 .userRole {
-  font-size: 10px;
+  font-family: var(--font-mono, ui-monospace, monospace);
+  font-size: 9px;
   color: var(--text-tertiary);
   font-weight: 500;
   text-transform: uppercase;
-  letter-spacing: 0.04em;
+  letter-spacing: 0.08em;
 }
 
 /* ── Main area ── */
@@ -138,36 +137,48 @@
   display: flex;
   flex-direction: column;
   overflow: hidden;
+  background: var(--bg-glow), var(--bg-page);
+  background-attachment: fixed;
 }
 
 /* ── Topbar / Header ── */
 .topbar {
   height: 52px;
   flex-shrink: 0;
-  background: rgba(10, 10, 10, 0.7);
+  background: rgba(10, 10, 10, 0.75);
   backdrop-filter: blur(12px);
   -webkit-backdrop-filter: blur(12px);
-  border-bottom: 1px solid var(--border);
+  border-bottom: 1px solid rgba(255, 255, 255, 0.06);
   display: flex;
   align-items: center;
   justify-content: space-between;
   padding: 0 20px 0 16px;
-  gap: 10px;
+  gap: 12px;
   position: sticky;
   top: 0;
   z-index: 100;
 }
 
+.topbarLeft {
+  display: flex;
+  align-items: center;
+  gap: 10px;
+  min-width: 0;
+  flex: 1;
+}
+
 .topbarBtn {
   padding: 4px 8px;
   height: 32px;
   width: 32px;
   color: var(--text-secondary) !important;
+  border-radius: 8px !important;
+  transition: background 120ms ease, color 120ms ease;
 }
 
 .topbarBtn:hover {
   color: var(--text-primary) !important;
-  background: rgba(255,255,255,0.06) !important;
+  background: rgba(255, 255, 255, 0.04) !important;
 }
 
 .topbarAction {
@@ -176,13 +187,44 @@
   height: 32px;
   min-width: 32px;
   width: auto !important;
-  border-radius: var(--radius-md) !important;
+  border-radius: 8px !important;
   transition: background 120ms ease, color 120ms ease;
 }
 
 .topbarAction:hover {
   color: var(--text-primary) !important;
-  background: rgba(255,255,255,0.06) !important;
+  background: rgba(255, 255, 255, 0.04) !important;
+}
+
+.searchTrigger {
+  display: inline-flex;
+  align-items: center;
+  gap: 8px;
+  padding: 0 10px;
+  height: 32px;
+  width: 240px;
+  max-width: 38vw;
+  background: rgba(255, 255, 255, 0.03);
+  border: 1px solid rgba(255, 255, 255, 0.06);
+  border-radius: 8px;
+  color: var(--text-tertiary);
+  font-size: 12px;
+  cursor: pointer;
+  transition: border-color 120ms ease, background 120ms ease, color 120ms ease;
+}
+
+.searchTrigger:hover {
+  border-color: rgba(255, 255, 255, 0.12);
+  background: rgba(255, 255, 255, 0.045);
+  color: var(--text-secondary);
+}
+
+.searchTriggerLabel {
+  flex: 1;
+  text-align: left;
+  white-space: nowrap;
+  overflow: hidden;
+  text-overflow: ellipsis;
 }
 
 .langFlag {
@@ -194,7 +236,7 @@
 }
 
 .topbarLabel {
-  font-size: 13px;
+  font-size: 12px;
   font-weight: 500;
   color: var(--text-secondary);
 }
@@ -203,32 +245,33 @@
   display: inline-flex;
   align-items: center;
   padding: 1px 5px;
-  background: var(--bg-elevated);
-  border: 1px solid var(--border);
+  background: rgba(255, 255, 255, 0.04);
+  border: 1px solid rgba(255, 255, 255, 0.08);
   border-radius: 4px;
   font-size: 10px;
   color: var(--text-tertiary);
-  margin-left: 4px;
-  font-family: var(--font-mono);
+  font-family: var(--font-mono, ui-monospace, monospace);
 }
 
 .logoutBtn {
   color: var(--text-secondary) !important;
-  font-size: 13px;
+  font-size: 12px;
   height: 32px;
-  transition: color 120ms ease;
+  border-radius: 8px !important;
+  transition: color 120ms ease, background 120ms ease;
 }
 
 .logoutBtn:hover {
   color: var(--error) !important;
-  background: var(--error-bg) !important;
+  background: rgba(239, 68, 68, 0.08) !important;
 }
 
 .toolbarDivider {
   width: 1px;
-  height: 20px;
-  background: var(--border);
+  height: 18px;
+  background: rgba(255, 255, 255, 0.08);
   flex-shrink: 0;
+  margin: 0 2px;
 }
 
 /* ── Page area ── */
@@ -240,4 +283,5 @@
 
 @media (max-width: 768px) {
   .pageArea { padding: 16px; }
+  .searchTrigger { display: none; }
 }
diff --git a/frontend/src/components/Layout/AppLayout.tsx b/frontend/src/components/Layout/AppLayout.tsx
index 14dde37..7ce509f 100644
--- a/frontend/src/components/Layout/AppLayout.tsx
+++ b/frontend/src/components/Layout/AppLayout.tsx
@@ -7,6 +7,7 @@ import Sidebar from './Sidebar';
 import MobileDrawer from './MobileDrawer';
 import NotificationBell from './NotificationBell';
 import FarmSwitcher from './FarmSwitcher';
+import Breadcrumbs from './Breadcrumbs';
 import OfflineIndicator from '../OfflineIndicator';
 import Logo from '../Logo';
 import CommandPalette from '../CommandPalette';
@@ -126,35 +127,44 @@ export default function AppLayout() {
       <div className={s.mainArea}>
         {/* Topbar */}
         <header className={s.topbar}>
-          {isMobile ? (
-            <Button
-              type="text"
-              icon={<MenuIcon size={18} />}
-              onClick={() => setDrawerOpen(true)}
-              className={s.topbarBtn}
-            />
-          ) : (
-            <Button
-              type="text"
-              icon={sidebarCollapsed
-                ? <PanelLeftOpen size={16} />
-                : <PanelLeftClose size={16} />
-              }
-              onClick={() => setSidebarCollapsed(prev => !prev)}
-              className={s.topbarBtn}
-            />
-          )}
+          <div className={s.topbarLeft}>
+            {isMobile ? (
+              <Button
+                type="text"
+                icon={<MenuIcon size={18} />}
+                onClick={() => setDrawerOpen(true)}
+                className={s.topbarBtn}
+              />
+            ) : (
+              <Button
+                type="text"
+                icon={sidebarCollapsed
+                  ? <PanelLeftOpen size={16} />
+                  : <PanelLeftClose size={16} />
+                }
+                onClick={() => setSidebarCollapsed(prev => !prev)}
+                className={s.topbarBtn}
+              />
+            )}
+            <Breadcrumbs />
+          </div>
 
           <div className={s.toolbarItem}>
-            <Button
-              type="text"
-              icon={<Search size={16} />}
-              onClick={openSearch}
-              className={s.topbarAction}
-              style={{ width: 'auto' }}
-            >
-              {!isMobile && <span className={s.topbarLabel}>{t.search.trigger} <kbd className={s.kbdHint}>⌘K</kbd></span>}
-            </Button>
+            {!isMobile && (
+              <button type="button" onClick={openSearch} className={s.searchTrigger}>
+                <Search size={14} aria-hidden="true" />
+                <span className={s.searchTriggerLabel}>{t.search.trigger}</span>
+                <kbd className={s.kbdHint}>⌘K</kbd>
+              </button>
+            )}
+            {isMobile && (
+              <Button
+                type="text"
+                icon={<Search size={16} />}
+                onClick={openSearch}
+                className={s.topbarAction}
+              />
+            )}
             <FarmSwitcher />
             <OfflineIndicator />
             <div className={s.toolbarDivider} />
diff --git a/frontend/src/components/Layout/Breadcrumbs.module.css b/frontend/src/components/Layout/Breadcrumbs.module.css
new file mode 100644
index 0000000..cface54
--- /dev/null
+++ b/frontend/src/components/Layout/Breadcrumbs.module.css
@@ -0,0 +1,50 @@
+.wrap {
+  display: flex;
+  align-items: center;
+  gap: 4px;
+  min-width: 0;
+  overflow: hidden;
+  font-family: var(--font-mono, ui-monospace, monospace);
+  font-size: 11px;
+  letter-spacing: 0.02em;
+  color: var(--text-tertiary);
+  white-space: nowrap;
+}
+
+.item {
+  display: inline-flex;
+  align-items: center;
+  gap: 4px;
+  min-width: 0;
+}
+
+.link {
+  color: var(--text-tertiary);
+  text-decoration: none;
+  transition: color 120ms ease;
+  padding: 2px 4px;
+  border-radius: 4px;
+}
+
+.link:hover {
+  color: var(--text-secondary);
+  background: rgba(255, 255, 255, 0.03);
+}
+
+.current {
+  color: var(--text-primary);
+  font-weight: 500;
+  padding: 2px 4px;
+  overflow: hidden;
+  text-overflow: ellipsis;
+  max-width: 240px;
+}
+
+.sep {
+  color: rgba(255, 255, 255, 0.22);
+  flex-shrink: 0;
+}
+
+@media (max-width: 640px) {
+  .wrap .item:not(:last-child) { display: none; }
+}
diff --git a/frontend/src/components/Layout/Breadcrumbs.tsx b/frontend/src/components/Layout/Breadcrumbs.tsx
new file mode 100644
index 0000000..fe7c200
--- /dev/null
+++ b/frontend/src/components/Layout/Breadcrumbs.tsx
@@ -0,0 +1,92 @@
+import { Link, useLocation } from 'react-router-dom';
+import { ChevronRight } from 'lucide-react';
+import { useTranslation } from '../../i18n';
+import s from './Breadcrumbs.module.css';
+
+export default function Breadcrumbs() {
+  const location = useLocation();
+  const { t } = useTranslation();
+
+  const segments = location.pathname.split('/').filter(Boolean);
+
+  // Map known route segments to friendly i18n labels with safe fallbacks.
+  const labelFor = (seg: string, fullPath: string): string => {
+    const n = (t.nav ?? {}) as Record<string, string>;
+    const exact: Record<string, string | undefined> = {
+      dashboard: n.dashboard,
+      fields: n.fields,
+      operations: n.operations,
+      machinery: n.machinery,
+      fleet: n.fleet,
+      warehouses: n.warehouses,
+      storage: n.grainModule,
+      fuel: n.fuelStation,
+      economics: n.finance,
+      sales: n.sales,
+      hr: n.hr,
+      analytics: n.analytics,
+      settings: n.settings,
+      admin: n.settings,
+      superadmin: n.settings,
+      profile: t.auth?.profile ?? 'Profile',
+      pnl: n.pnl,
+      budget: n.budget,
+      leases: n.leases,
+      employees: n.employees,
+      worklogs: n.workLogs,
+      salary: n.salary,
+      efficiency: n.efficiency,
+      resources: n.resources,
+      marginality: n.marginality,
+      'rotation-advisor': n.cropRotationAdvisor,
+      items: n.materials,
+      users: n.users,
+      audit: n.auditLog,
+      'role-permissions': n.rolePermissions,
+      approvals: n.approvals,
+      'approval-rules': n.approvalRules,
+      'api-keys': n.apiKeys,
+      companies: n.companies,
+    };
+    if (exact[seg]) return exact[seg]!;
+    // numeric or uuid → "#"
+    if (/^\d+$/.test(seg)) return `#${seg}`;
+    if (/^[0-9a-f-]{8,}$/i.test(seg)) return '…';
+    void fullPath;
+    return seg.charAt(0).toUpperCase() + seg.slice(1).replace(/-/g, ' ');
+  };
+
+  // Build href chain
+  const crumbs = segments.map((seg, idx) => {
+    const path = '/' + segments.slice(0, idx + 1).join('/');
+    return { seg, path, label: labelFor(seg, path) };
+  });
+
+  if (crumbs.length === 0) {
+    return (
+      <nav className={s.wrap} aria-label="breadcrumb">
+        <span className={s.current}>{(t.nav as Record<string, string>)?.dashboard ?? 'Dashboard'}</span>
+      </nav>
+    );
+  }
+
+  return (
+    <nav className={s.wrap} aria-label="breadcrumb">
+      {crumbs.map((c, i) => {
+        const isLast = i === crumbs.length - 1;
+        return (
+          <span key={c.path} className={s.item}>
+            {isLast ? (
+              <span className={s.current}>{c.label}</span>
+            ) : (
+              <Link to={c.path} className={s.link}>{c.label}</Link>
+            )}
+            {!isLast && (
+              <ChevronRight size={11} className={s.sep} aria-hidden="true" />
+            )}
+          </span>
+        );
+      })}
+    </nav>
+  );
+}
diff --git a/frontend/src/components/Layout/MobileDrawer.module.css b/frontend/src/components/Layout/MobileDrawer.module.css
index ebe1c04..3a008d1 100644
--- a/frontend/src/components/Layout/MobileDrawer.module.css
+++ b/frontend/src/components/Layout/MobileDrawer.module.css
@@ -1,3 +1,10 @@
 .colored {
   color: var(--text-primary);
 }
+
+/* Backdrop blur on the AntD mask under the drawer */
+:global(.ant-drawer) :global(.ant-drawer-mask) {
+  background: rgba(0, 0, 0, 0.6) !important;
+  backdrop-filter: blur(4px);
+  -webkit-backdrop-filter: blur(4px);
+}
diff --git a/frontend/src/components/Layout/Sidebar.module.css b/frontend/src/components/Layout/Sidebar.module.css
index ee204e8..6e7a46b 100644
--- a/frontend/src/components/Layout/Sidebar.module.css
+++ b/frontend/src/components/Layout/Sidebar.module.css
@@ -9,83 +9,67 @@
   border-right: 0;
   flex: 1;
   overflow-y: auto;
-  background: transparent;
+  background: transparent !important;
 }
 
 /* ── Menu item base ── */
-.bg :global(.ant-menu-item) {
-  height: 36px;
-  line-height: 36px;
-  padding: 0 12px !important;
-  border-radius: var(--radius-md);
+.bg :global(.ant-menu-item),
+.bg :global(.ant-menu-submenu-title) {
+  height: 36px !important;
+  line-height: 36px !important;
+  padding: 0 10px !important;
+  border-radius: 8px;
   margin: 1px 0;
-  font-size: 13px;
-  font-weight: 400;
-  color: var(--text-secondary);
-  transition: all 120ms ease;
+  font-size: 12px !important;
+  font-weight: 400 !important;
+  color: rgba(255, 255, 255, 0.58) !important;
+  transition: background 150ms ease, color 150ms ease;
   position: relative;
 }
 
-.bg :global(.ant-menu-item:hover) {
-  background: rgba(255, 255, 255, 0.05) !important;
-  color: var(--text-primary) !important;
+.bg :global(.ant-menu-item:hover),
+.bg :global(.ant-menu-submenu-title:hover) {
+  background: rgba(255, 255, 255, 0.03) !important;
+  color: rgba(255, 255, 255, 0.94) !important;
 }
 
-/* ── Active item: accent dot at right with glow ── */
+/* ── Active item: left bar at edge in --acc ── */
 .bg :global(.ant-menu-item-selected) {
-  background: rgba(255, 255, 255, 0.06) !important;
-  color: var(--text-primary) !important;
-  font-weight: 500;
+  background: rgba(var(--accRgb), 0.12) !important;
+  color: var(--acc) !important;
+  font-weight: 500 !important;
   border-inline-end: none !important;
 }
 
 .bg :global(.ant-menu-item-selected::after) {
   content: '';
   position: absolute;
-  right: 0;
-  top: 50%;
-  transform: translateY(-50%);
-  width: 4px;
-  height: 16px;
-  background: #22C55E;
-  border-radius: 4px;
-  box-shadow: 0 0 8px rgba(34, 197, 94, 0.4);
+  left: -8px;
+  top: 4px;
+  bottom: 4px;
+  width: 2px;
+  background: var(--acc);
+  border-radius: 0 2px 2px 0;
+  box-shadow: 0 0 8px rgba(var(--accRgb), 0.55);
 }
 
 .bg :global(.ant-menu-item-selected::before) {
   display: none;
 }
 
-/* Active icon color */
-.bg :global(.ant-menu-item-selected .ant-menu-item-icon) {
-  color: var(--text-primary) !important;
-}
-
-/* ── Submenu title ── */
-.bg :global(.ant-menu-submenu-title) {
-  height: 36px !important;
-  line-height: 36px !important;
-  padding: 0 12px !important;
-  border-radius: var(--radius-md);
-  margin: 1px 0;
-  font-size: 13px;
-  font-weight: 400;
-  color: var(--text-secondary);
-  transition: all 120ms ease;
-}
-
-.bg :global(.ant-menu-submenu-title:hover) {
-  background: rgba(255, 255, 255, 0.05) !important;
-  color: var(--text-primary) !important;
+.bg :global(.ant-menu-item-selected .ant-menu-item-icon),
+.bg :global(.ant-menu-item-selected .anticon) {
+  color: var(--acc) !important;
 }
 
+/* ── Submenu open/selected ── */
 .bg :global(.ant-menu-submenu-selected > .ant-menu-submenu-title) {
-  color: var(--text-primary) !important;
-  font-weight: 500;
+  color: rgba(255, 255, 255, 0.94) !important;
+  font-weight: 500 !important;
 }
 
 .bg :global(.ant-menu-submenu-open > .ant-menu-submenu-title) {
-  color: var(--text-primary) !important;
+  color: rgba(255, 255, 255, 0.94) !important;
 }
 
 /* ── Expand arrow ── */
@@ -97,17 +81,17 @@
 .bg :global(.ant-menu-item-only-child) {
   padding-left: 36px !important;
   font-size: 12px;
-  color: var(--text-tertiary);
-  height: 32px;
-  line-height: 32px;
+  color: rgba(255, 255, 255, 0.5) !important;
+  height: 32px !important;
+  line-height: 32px !important;
 }
 
 .bg :global(.ant-menu-item-only-child:hover) {
-  color: var(--text-primary) !important;
+  color: rgba(255, 255, 255, 0.94) !important;
 }
 
 .bg :global(.ant-menu-item-only-child.ant-menu-item-selected) {
-  color: var(--text-primary) !important;
+  color: var(--acc) !important;
   font-size: 12px;
 }
 
@@ -115,6 +99,7 @@
 .bg :global(.ant-menu-item .ant-menu-item-icon),
 .bg :global(.ant-menu-submenu-title .ant-menu-item-icon) {
   margin-inline-end: 10px !important;
+  font-size: 14px !important;
 }
 
 .bg :global(.ant-menu-item .ant-menu-item-icon + span),
@@ -124,8 +109,8 @@
 
 /* ── Section dividers ── */
 .bg :global(.ant-menu-item-divider) {
-  border-color: var(--border) !important;
-  margin: 6px 0 !important;
+  border-color: rgba(255, 255, 255, 0.06) !important;
+  margin: 8px 4px !important;
 }
 
 /* ── Submenu popup (collapsed mode) background ── */
@@ -134,11 +119,12 @@
 }
 
 .sectionLabel {
-  font-size: 10px;
+  font-family: var(--font-mono, ui-monospace, monospace);
+  font-size: 9px;
   font-weight: 600;
   text-transform: uppercase;
   letter-spacing: 0.08em;
   color: var(--text-tertiary);
-  padding: 14px 20px 4px;
+  padding: 14px 10px 4px;
   user-select: none;
 }
diff --git a/frontend/src/pages/Dashboard.module.css b/frontend/src/pages/Dashboard.module.css
index adabf01..18d9316 100644
--- a/frontend/src/pages/Dashboard.module.css
+++ b/frontend/src/pages/Dashboard.module.css
@@ -9,6 +9,144 @@
   gap: 12px;
 }
 
+/* ── Premium page header ── */
+.pageHead {
+  display: flex;
+  align-items: flex-start;
+  justify-content: space-between;
+  flex-wrap: wrap;
+  gap: 18px;
+  margin-bottom: 22px;
+}
+
+.headLeft {
+  display: flex;
+  flex-direction: column;
+  gap: 10px;
+  min-width: 0;
+}
+
+.headTitle {
+  margin: 0;
+  font-size: clamp(24px, 3vw, 30px);
+  font-weight: 600;
+  line-height: 1.15;
+  letter-spacing: -0.03em;
+  background: linear-gradient(180deg, #ffffff 0%, rgba(255, 255, 255, 0.65) 100%);
+  -webkit-background-clip: text;
+  background-clip: text;
+  -webkit-text-fill-color: transparent;
+  color: transparent;
+}
+
+.headSubMeta {
+  margin: 0;
+  display: inline-flex;
+  flex-wrap: wrap;
+  align-items: center;
+  gap: 6px;
+  font-family: var(--font-mono, ui-monospace, monospace);
+  font-size: 12px;
+  color: var(--text-secondary);
+  letter-spacing: 0.01em;
+}
+
+.dotSep {
+  color: var(--text-tertiary);
+}
+
+.headRight {
+  display: flex;
+  align-items: center;
+  gap: 10px;
+  flex-wrap: wrap;
+}
+
+.weatherSlot {
+  display: inline-flex;
+}
+
+@media (max-width: 768px) {
+  .weatherSlot { display: none; }
+}
+
+/* ── Segmented period control ── */
+.segmented {
+  display: inline-flex;
+  align-items: center;
+  padding: 3px;
+  background: rgba(255, 255, 255, 0.025);
+  border: 1px solid rgba(255, 255, 255, 0.08);
+  border-radius: 999px;
+  gap: 2px;
+}
+
+.seg {
+  appearance: none;
+  background: transparent;
+  border: 0;
+  height: 26px;
+  padding: 0 12px;
+  border-radius: 999px;
+  font-family: var(--font-mono, ui-monospace, monospace);
+  font-size: 11px;
+  letter-spacing: 0.04em;
+  text-transform: uppercase;
+  color: var(--text-tertiary);
+  cursor: pointer;
+  transition: background 150ms ease, color 150ms ease;
+}
+
+.seg:hover {
+  color: var(--text-primary);
+}
+
+.segActive {
+  background: rgba(255, 255, 255, 0.08);
+  color: var(--text-primary);
+  box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.06);
+}
+
+/* ── Primary CTA ── */
+.ctaPrimary {
+  display: inline-flex;
+  align-items: center;
+  gap: 8px;
+  height: 32px;
+  padding: 0 14px;
+  border-radius: 8px;
+  border: 0;
+  cursor: pointer;
+  background: linear-gradient(180deg, #22C55E 0%, #16A34A 100%);
+  color: #000;
+  font-size: 12px;
+  font-weight: 600;
+  letter-spacing: 0.01em;
+  box-shadow:
+    0 1px 0 rgba(255, 255, 255, 0.18) inset,
+    0 0 0 1px rgba(var(--accRgb), 0.35),
+    0 8px 24px -8px rgba(var(--accRgb), 0.45);
+  transition: transform 150ms ease, box-shadow 150ms ease, filter 150ms ease;
+}
+
+.ctaPrimary:hover {
+  transform: translateY(-1px);
+  filter: brightness(1.04);
+  box-shadow:
+    0 1px 0 rgba(255, 255, 255, 0.22) inset,
+    0 0 0 1px rgba(var(--accRgb), 0.45),
+    0 12px 32px -8px rgba(var(--accRgb), 0.6);
+}
+
+.ctaPrimary:active {
+  transform: translateY(0);
+}
+
+@media (prefers-reduced-motion: reduce) {
+  .ctaPrimary { transition: none; }
+  .ctaPrimary:hover { transform: none; }
+}
+
 /* ── KPI grid ── */
 .kpiGrid {
   display: grid;
diff --git a/frontend/src/pages/Dashboard.tsx b/frontend/src/pages/Dashboard.tsx
index 3ec00b2..4b2c437 100644
--- a/frontend/src/pages/Dashboard.tsx
+++ b/frontend/src/pages/Dashboard.tsx
@@ -1,11 +1,11 @@
-import { useEffect } from 'react';
+import { useEffect, useState } from 'react';
 import { message } from 'antd';
 import { KpiSkeleton, ChartSkeleton, TableSkeleton as TableSkeletonNew } from '../components/Skeletons';
-import { Clipboard, Fuel, Wheat, Receipt, Map, Activity, Banknote, Users } from 'lucide-react';
+import { Clipboard, Fuel, Wheat, Receipt, Map, Activity, Banknote, Users, ArrowRight } from 'lucide-react';
 import { useNavigate, Navigate } from 'react-router-dom';
 import type { FieldDto } from '../types/field';
 import type { AgroOperationDto } from '../types/operation';
-import PageHeader from '../components/PageHeader';
+import EyebrowChip from '../components/EyebrowChip';
 import WeatherWidget from '../components/WeatherWidget';
 import AlertsPanel from '../components/dashboard/AlertsPanel';
 import { useTranslation } from '../i18n';
@@ -29,6 +29,7 @@ export default function Dashboard() {
   const role = useAuthStore((s) => s.role);
   const hasCompletedOnboarding = useAuthStore((s) => s.hasCompletedOnboarding);
 
+  const [period, setPeriod] = useState<'day' | 'week' | 'month' | 'season'>('season');
   const { data, isLoading: dashLoading, isError: dashError } = useDashboardQuery();
   const { data: fieldsData, isLoading: fieldsLoading } = useDashboardFieldsQuery();
   const { data: operationsData, isLoading: opsLoading } = useDashboardOperationsQuery();
@@ -121,13 +122,56 @@ export default function Dashboard() {
         { label: t.dashboard.quickCost, icon: <Receipt size={16} />, color: '#8B5CF6', route: '/economics' },
       ];
 
+  const periodOptions: Array<{ key: typeof period; label: string }> = [
+    { key: 'day', label: t.dashboard.day ?? 'День' },
+    { key: 'week', label: t.dashboard.week ?? 'Тиждень' },
+    { key: 'month', label: t.dashboard.month ?? 'Місяць' },
+    { key: 'season', label: t.dashboard.season ?? 'Сезон' },
+  ];
+
   return (
     <div className="page-enter">
-      {/* Header + Weather */}
-      <div className={s.flex_between_wrap}>
-        <PageHeader title={t.dashboard.title} subtitle={t.dashboard.subtitle} />
-        <WeatherWidget />
-      </div>
+      {/* Premium header */}
+      <header className={s.pageHead}>
+        <div className={s.headLeft}>
+          <EyebrowChip label="DASHBOARD / SEASON 2026" />
+          <h1 className={s.headTitle}>{t.dashboard.title}</h1>
+          <p className={s.headSubMeta}>
+            <span>{t.dashboard.subtitle}</span>
+            <span className={s.dotSep}>·</span>
+            <span>{formatUA(data.totalAreaHectares)} {t.dashboard.haUnit ?? 'га'}</span>
+            <span className={s.dotSep}>·</span>
+            <span>{fields.length} {t.dashboard.fieldsUnit ?? 'полів'}</span>
+          </p>
+        </div>
+        <div className={s.headRight}>
+          <div className={s.segmented} role="tablist" aria-label="Period">
+            {periodOptions.map((opt) => (
+              <button
+                key={opt.key}
+                role="tab"
+                aria-selected={period === opt.key}
+                className={`${s.seg} ${period === opt.key ? s.segActive : ''}`}
+                onClick={() => setPeriod(opt.key)}
+                type="button"
+              >
+                {opt.label}
+              </button>
+            ))}
+          </div>
+          <button
+            type="button"
+            className={s.ctaPrimary}
+            onClick={() => navigate('/operations')}
+          >
+            <span>{t.dashboard.newOperation ?? 'Нова операція'}</span>
+            <ArrowRight size={14} aria-hidden="true" />
+          </button>
+          <div className={s.weatherSlot}>
+            <WeatherWidget />
+          </div>
+        </div>
+      </header>
 
       {/* KPI Hero Row */}
       <KpiHeroRow items={kpiItems} />
